/**
 * APEX Research Terminal — browser-side API keys
 *
 * This lightweight stub exists so that /finance-ai.js always returns 200
 * in both local and Render deployments. The real live data paths now go
 * through finance-ai/server.py using .env keys on the backend.
 *
 * You can optionally populate these for direct browser calls, but it is
 * NOT required for the dashboard to function.
 */
window.APEX_KEYS = {
  alphaVantage: '',
  finnhub: '',
  fred: ''
};

/**
 * APEX Research Terminal — API keys (keep private; add finance-ai.js to .gitignore)
 * Get free keys: Alpha Vantage (alphavantage.co), Finnhub (finnhub.io), FRED (fred.stlouisfed.org)
 */
window.APEX_KEYS = {
  alphaVantage: '',
  finnhub: '',
  fred: ''
};
